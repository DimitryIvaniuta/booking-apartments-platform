# booking-apartments-platform
Production-grade apartment booking service (Java 21, Spring Boot 3.5.10) with anti-double-booking concurrency via Postgres exclusion constraints, JWT auth + refresh token families, Redis rate limiting/caching/revocation/lockout, transactional outbox + Kafka events, Flyway seeds, and full integration tests + Postman collections.
